Roboto 2.138, from AOSP external/roboto-fonts (https://github.com/google/roboto/archive/v2.136.zip).
Apache License 2.0 -- see NOTICE.

These are the twelve faces res/fonts.xml names for the sans-serif family, so
the generic names and their weight aliases resolve to the same faces Android
uses instead of whatever the host fontconfig substitutes.
